

const fiil = (w, h, template) => {

  const area = w * h
  const g = new Array(area)


  const next = () => {
    // insert the next node in grid place
    // link it to the previous node for back tracing

    // backtrace:
    // point to previous node and delete current node
    // pop the next choice for previous node
    // then continue
  }

}